{
  "$schema": "https://json-schema.org/schema",
  "$id": "Dialog",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },

    "path": {
      "type": "string",
      "description": "nestet path to create dialog",
      "format": "path",
      "visible": true,
      "$default": {
        "$source": "workingDirectory"
      }
    }
  },
  "required": ["name"]
}
