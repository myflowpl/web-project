import { TopOutletDirective } from './top-outlet.directive';

describe('TopOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new TopOutletDirective();
    expect(directive).toBeTruthy();
  });
});
