import { TopContentDirective } from './top-content.directive';

describe('TopContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TopContentDirective();
    expect(directive).toBeTruthy();
  });
});
