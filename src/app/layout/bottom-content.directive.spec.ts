import { BottomContentDirective } from './bottom-content.directive';

describe('BottomContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomContentDirective();
    expect(directive).toBeTruthy();
  });
});
