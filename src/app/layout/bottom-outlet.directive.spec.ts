import { BottomOutletDirective } from './bottom-outlet.directive';

describe('BottomOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomOutletDirective();
    expect(directive).toBeTruthy();
  });
});
